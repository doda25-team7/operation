apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.model.name }}
  labels:
    app: {{ .Release.Name }}-{{ .Values.model.name }}
    version: stable   
spec:
  replicas: {{ .Values.model.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-{{ .Values.model.name }}
      version: stable
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-{{ .Values.model.name }}
        version: stable
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: {{ .Values.model.name }}
          image: {{ .Values.model.image }}
          ports:
            - containerPort: {{ .Values.model.port }}
              name: http
            {{- if and (hasKey .Values.model "metrics") .Values.model.metrics.enabled (ne .Values.model.metrics.port .Values.model.port) }}
            - containerPort: {{ .Values.model.metrics.port }}
              name: metrics
            {{- end }}
          env:
            - name: MODEL_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Release.Name }}-config
                  key: MODEL_PORT
            - name: MODEL_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Release.Name }}-config
                  key: MODEL_URL